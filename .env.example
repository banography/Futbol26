# Copy this file to .env and fill in real values.
# Never commit .env — it is gitignored.

# Optional: backend URL — when set, futbolApi.ts will call this instead of bundled data
EXPO_PUBLIC_API_URL=
